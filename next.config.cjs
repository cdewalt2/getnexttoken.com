/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  pageExtensions: ['ts', 'tsx', 'js', 'jsx'], // Ensure Next.js recognizes all types of pages
};

module.exports = nextConfig;
